#Project: EmployeeManagement

SWAGGER is implemented to test the available APIs

EmployeeController and AuthorizationTokenController.

Authorization is done using auth0 token, bearer token can be generated from AuthorizationTokenController which needs to be provided 

as input in the Authorization dialog.

Steps for setup
--------------
1. Run SQL query in LOCAL MS SQL SERVER (script available in SqlScriptForDB.sql file)
2. Set EmployeeManagement.WebAPI as startup project


